import { ethereumTokens } from "@/configs/pools/tokenConfigs/ethereumTokens";
import { kavaTokens } from "@/configs/pools/tokenConfigs/kavaTokens";
import { arbitrumTokens } from "@/configs/pools/tokenConfigs/arbitrumTokens";
import { blastTokens } from "@/configs/pools/tokenConfigs/blastTokens";

export const tokenConfigs = {
  1: ethereumTokens,
  2222: kavaTokens,
  42161: arbitrumTokens,
  81457: blastTokens,
};
